{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["swagger.json", "tsoa.json", "routes.ts", "dist", "node_modules"],
  "exec": "npm run tsoa-routes && npm run tsoa-spec && npm run build && node dist/main.js"
}
